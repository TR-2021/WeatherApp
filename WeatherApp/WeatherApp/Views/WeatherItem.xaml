<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:WeatherApp.Views"
             x:Class="WeatherApp.Views.WeatherItem">
    <ContentView.Content>
            <FlexLayout Direction="{Binding ItemDirection}" x:Name="flexLayout" JustifyContent="SpaceBetween" AlignItems="Center" >
                <Label TextColor="{Binding TextColor}"  x:Name="timeLabel" Text="{Binding WeatherTime}" />
            <views:WeatherIcon x:Name="weatherIcon" WeatherType="{Binding WeatherType, Converter={StaticResource converter}}"/>
            <StackLayout Orientation="Horizontal" Spacing="20">
                <Label TextColor="{Binding TextColor}" x:Name="temperatureLabel" Text="{Binding Temperature}"/>
                <Label TextColor="{Binding TextColor}" x:Name="humidityLabel" Text="{Binding Humidity}" IsVisible="{Binding HumidityVisible}"/>
            </StackLayout>
        </FlexLayout>
    </ContentView.Content>
    
</ContentView>
<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:WeatherApp.Views"
             xmlns:local="clr-namespace:WeatherApp"
             x:Class="WeatherApp.Views.WeatherItem">

  <ContentView.Content>
        <FlexLayout Direction="{Binding ItemDirection}" x:Name="flexLayout" JustifyContent="SpaceBetween" AlignItems="Center">
            <Label x:Name="timeLabel" Text="{Binding WeatherTime}"/>
            <views:WeatherIcon x:Name="weatherIcon" WeatherType="{Binding WeatherType}"/>
            <Label x:Name="temperatureLabel" Text="{Binding Temperature}"/>
        </FlexLayout>
    </ContentView.Content>
</ContentView>